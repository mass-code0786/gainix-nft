import { gainixChains, gainixDefaultChain, gainixTestChain } from "@/lib/web3/chains";

export const contractChains = gainixChains;
export const contractDefaultChain = gainixDefaultChain;
export const contractTestChain = gainixTestChain;
