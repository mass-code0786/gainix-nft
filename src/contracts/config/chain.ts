import {
  gainixActiveChain,
  gainixActiveChainId,
  gainixChains,
  gainixDefaultChain,
  gainixMainnetChain,
} from "@/lib/web3/chains";

export const contractChains = gainixChains;
export const contractDefaultChain = gainixDefaultChain;
export const contractMainnetChain = gainixMainnetChain;
export const contractActiveChain = gainixActiveChain;
export const contractActiveChainId = gainixActiveChainId;
